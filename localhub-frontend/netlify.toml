# Netlify 배포 설정 — repo 연동 배포용
# base 를 localhub-frontend 로 두면 모노레포 루트에서 이 하위폴더를 빌드합니다.
[build]
  base = "localhub-frontend"
  command = "npm run build"
  publish = "dist"

# SPA(History 모드) — 모든 경로를 index.html 로 폴백
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
